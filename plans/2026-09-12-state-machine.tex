\documentclass[11pt,a4paper]{article}
\usepackage[margin=22mm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs,tabularx,array}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric}
\usepackage[hidelinks]{hyperref}
\usepackage{xcolor}
\setlength{\parskip}{4pt}
\newcommand{\st}[1]{\textsc{#1}}
\newcommand{\file}[1]{\texttt{#1}}
\title{The Pathfinder engine}
\author{Pathfinder, \file{github.com/vd1/pathfinder}}
\date{12 September 2026, revised 13 September}
\begin{document}
\maketitle

\begin{abstract}
  
\end{abstract}

\section{Introduction}

\paragraph{The pathfinder program.} Pathfinder takes two corpora of scientific
papers (concretely arxiv deposits), $Q$ and $P$, and, using agentic methods, looks for
pairs $(q, p)$ in $Q\times P$ with enough of an interesting connexion to try and
elaborate it as a pre-publication. Ideally, the pre-publication satisfies the
usual constraints of correctness, novelty, relevance, readability, and
exposition of related work in the literature. 

In some cases, the input tuple may be supplemented with a seed note (provided by
the agent that scanned and scored the pair in the first place) that is a
paragraph describing the putative connexion.

This type of task is commonly carried out by human experts. It happens through
seminars that the seed of an idea is provided by someone in the audience, and
such connexions are made, and, in favourable circumstances, further pursued all
the way to a publication. However, by using modern agents (which are competent
experts on every corpus, and always ``willing'' to contribute), one can do it
faster, cheaper, and, therefore, at a larger scale. We have ran it on sets of 
paper of size about a hundred each for a reasonable token budget.

A small epistemologic aside: Pathfinder is about scientific creativity,
(cite Swanson, Cedric Lions, and other refs on creativity by analogy) 
less about closing conjectures, a task at which modern agents have become so good.
Whether this particular attempt at generative creative input is enough or at all capable
of creating new accumulative knowledge remains to be seen.

\paragraph{Object of this note.}
This note purports to describe in implementable terms
a simple agentic process whereby an input tuple
is progressively elaborated and in the favourable cases a pre-publication is
generated. 

As we will see below, the process recruits various agents in various
roles: researchers, verifiers, editors, etc. It consists broadly in two
phases.
\begin{enumerate}
\item The research phase: 
researcher agents collaborate and produce a draft paper via
rounds of interactions with a verifier agent. This is the creative
part of the process which can end on paused thread, or a draft. 
In fact, running this phase again may lead to a different
outcome, possibly a draft expounding on a different idea. 
(The next paragraph gives an example.)
\item The editing phase: 
an author agent produces via rounds of
interactions with a reviewer agent, a final version of the draft.
The final draft can put on pause or accepted as a pre-publication 
by the reviewer.
\end{enumerate}

\paragraph{An example, and why reruns differ.}
Consider the following $(q,p,c)$ input triplet (where $c$ was written by the scanning agent):

\begin{quote}
$q=\texttt{arXiv:2608.29130}$ ``A Systematic Approach to Mechanism Design with Stochastic Dynamic
Stability'', which designs quadratic payments so
that self-interested agents settle at the welfare-maximising allocation and
learns that equilibrium from sampled feedback

$p=\texttt{arXiv:2609.04460}$ ``Distributed
risk-averse optimization via CVaR'', which minimises
conditional value-at-risk over a network from noisy function values. 

The seed $c$ is: 
replace $q$'s expected-value objectives with
CVaR and use $p$'s gradient estimator inside $q$'s learning loop; does the
payment mechanism still work?
\end{quote}

The thread was run twice on $(q,p,c)$. Both runs quickly rejected the
literal splice ($q$ needs a best response, $p$ supplies a gradient step) and
both ended as \textsc{draft}, but with different results. The first run
proved a positive statement: a uniform bound
\(
\sup_x |\widetilde C_n(x) - C_n(x)| \le L_n\delta_n + (U_n/\alpha_n)(2\pi/s_n)^{1/2}
\)
between $p$'s smoothed, sampled surrogate and the true CVaR objective, and
drew from it welfare and individual-rationality guarantees for the
risk-averse mechanism. The second run proved obstructions instead: a
two-point example in which the expected objective is strongly concave but
its CVaR is constant, so the curvature $q$'s uniqueness proof needs can
vanish under the substitution, and a coupling example showing that the
local risk oracles both papers use cannot determine system-wide tail risk.
Each note is correct, each was accepted by a verifier that had not taken
part.

Thus, the research phase is a sampler. The same input, rerun, may
elicit a different response. Arguably, in the example above, 
a referee would want both responses in one paper.

\paragraph{The various stages of the process}
The process produces the following sequence of file outputs:
\begin{enumerate}
\item 
a conversation \emph{thread} recorded on a ledger of everything the researcher 
agents said to each other
\item a \emph{note} consigning to paper form what the researcher agents and verifier 
agreed on (together with a \emph{readable note} of the same, rewritten for a reader 
who never saw the ledger), and,
\item
when accepted by the reviewer, a \emph{paper} 
% (written
% with a literature search and reviewed)
\end{enumerate}
The outputs above are arranged in increasing order of readability by humans, and 
decreasing dependence on the history of the generation process.

The readable note presents the research phase’s final outcome for expert assessment,
including unsuccessful or inconclusive outcomes.


\paragraph{Why a state machine.} The work is done by model calls that cost money, take minutes,
fail, and disagree. Writing the pipeline as a machine with explicit states, transitions and
budgets is what makes it re-implementable by a colleague on another corpus, auditable after the
fact (every decision traces to a file and a receipt), and safe to interrupt: a stop, a crash or an
exhausted budget leaves the machine in a named state from which it resumes, and a human can see
at a glance where each pair stands and what would move it.

\paragraph{How.} One research thread is one instance of the machine described below. Its subject
is a pair of papers together with the seed of an idea: in Pathfinder the seed is the scan's
connexion, rationale and scores, handed to the peers in their first prompt; in the agQSL instance
of the same machine the seed is a written idea artefact. The machine does not care where the seed
came from, only that the thread starts from a pair and a seed. The campaign is a set of these
instances plus a scheduler with a purse.
Three kinds of actor appear: \emph{agents}, model calls with a prompt and a bounded view of the
state, of which the two \emph{judges} (the verifier of a note, the reviewer of a paper) make the
only decisions; the \emph{pipeline}, code that builds, checks, records and moves the position;
and the \emph{owner}, whoever types in the terminal, a human or a delegated session, who raises
budgets, edits by hand and settles what is paused. The agents of one thread share one append-only
ledger; that ledger, the files beside it and a position record are the whole state of the
thread. Everything below is what the code does; the builder prompt points here for the shape to
re-implement.

\section{Global state of a thread}

\paragraph{Position.} \file{stage}, where the machine is; \file{status}, one of \file{running},
\file{stopped}, \file{BLOCKED} or a terminal word; the counters \file{round} (research rounds
used), \file{repairs} (note repairs used), the paper's \file{round} and the editor's
\file{attempt}.

\paragraph{Memory,} all files under \file{threads/<pair>/}:
\begin{itemize}\setlength{\itemsep}{1pt}
  \item \file{inputs/}: the two papers, flattened \TeX{} or PDF text, and their metadata; the
    seed, the pair's row of \file{scan.jsonl}, is read from the campaign level.
  \item \file{ledger.jsonl}: append-only attributed entries of kind idea, finding, objection,
    correction, intention, ready or review; the peers' shared memory and the verifier's evidence.
  \item one directory per peer (\file{ada/}, \file{emmy/}, \ldots): that peer's derivations,
    scripts and checks. The peers are a campaign setting (\file{peers}); two by default, three
    were tested.
  \item \file{<pair>.tex}: the consolidated note; \file{<pair>.verdict.json}: every verdict with
    the digest of the note it judged.
  \item \file{paper/}: the paper, its bibliography, its search record, the pipeline's checks per
    round, and every review with the digest of the paper it judged.
  \item \file{edited/}: the readable note (\file{note.tex}, \file{references.bib},
    \file{note.pdf}).
  \item \file{status.json}, \file{paper/paper.json}, \file{edited/edit.json}: the position of
    each stage; \file{lock}: the pid of the runner working the thread.
\end{itemize}

\paragraph{Campaign level.} \file{fetch.json} (the two queries and how far each side has been
paged), \file{Q.jsonl} and \file{P.jsonl}, \file{scan.jsonl}, \file{shortlist.json},
\file{receipts.jsonl} (the only spend figure), and the markers \file{stop.json} and
\file{health.json}.

\section{The ledger}

The ledger is the one piece of state several agents write. Its rules are what make the peers a
group rather than a set of monologues:
\begin{itemize}\setlength{\itemsep}{1pt}
  \item Entries are appended through a helper, never edited; each carries a sequence number, an
    actor, a kind and a time. Peers read the whole ledger before every call.
  \item \emph{ready} is a claim about the ledger as it stands: it is void as soon as anyone
    adds a substantive entry (anything but ready or review) after it. A peer whose ready still
    stands is not called again in that round; it waits for its partners.
  \item The round ends when every peer's ready stands, or when the round's allowance is out.
  \item The verifier writes its verdict into the ledger as a review entry, so the next round's
    peers, and the consolidator on a repair, read the judge's reasons where they read everything
    else.
\end{itemize}

\section{States}

\begin{center}
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[x=1cm, y=1cm,
  every node/.style={font=\footnotesize},
  state/.style={draw, rounded corners=2pt, minimum height=6.5mm, inner xsep=6pt, fill=gray!8},
  term/.style={state, fill=green!12},
  pause/.style={state, fill=orange!14},
  edge/.style={-{Stealth[length=2mm]}, semithick},
  lab/.style={font=\scriptsize, fill=white, inner sep=1.5pt}]
  \node[align=center] (input) at (2.4,0) {incoming message $(p,q,c)$\\$c$: optional seed connexion};
  \node[state] (peers) at (6.2,0) {\st{research}};
  \node[state] (cons) at (11.4,0) {\st{consolidate}};
  \node[state] (ver) at (15.6,0) {\st{verify}};
  \node[pause] (poi) at (21,1.6) {\st{pause-on-iterate}};
  \node[pause] (por) at (21,0) {\st{pause-on-revise}};
  \node[pause] (pause) at (21,-1.6) {\st{pause}};
  \node[term] (draft) at (21,-3.2) {\st{draft}};
  \draw[edge] (input) -- (peers);
  \draw[edge] (peers) -- node[lab, above, align=center] {ledger converged} (cons);
  \draw[edge] (cons) -- node[lab, above] {note written} (ver);
  \draw[edge] (ver) to[out=120, in=60, looseness=0.7] node[lab, above] {\st{iterate}: another research round} (peers);
  \draw[edge] (ver) to[out=225, in=315, looseness=1.0] node[lab, below] {\st{revise}: another repair round} (cons);
  \draw[edge] (ver.east) -- node[lab, sloped, below, pos=0.6] {\st{draft}} (draft.west);
  \draw[edge] (ver.east) -- node[lab, sloped, below, pos=0.6] {\st{pause}} (pause.west);
  \draw[edge] (ver.east) -- node[lab, above, pos=0.6] {\st{revise}, repairs out} (por.west);
  \draw[edge] (ver.east) -- node[lab, sloped, above, pos=0.6] {\st{iterate}, rounds out} (poi.west);
  \draw[edge] (peers.south) -- (6.2,-4.4) -- node[lab, below] {empty ledger} (23.4,-4.4) |- (pause.east);
\end{tikzpicture}}

\medskip

\begin{tikzpicture}[x=1cm, y=1cm,
  every node/.style={font=\footnotesize},
  state/.style={draw, rounded corners=2pt, minimum height=6.5mm, inner xsep=6pt, fill=gray!8},
  term/.style={state, fill=green!12},
  pause/.style={state, fill=orange!14},
  blocked/.style={state, fill=red!10},
  edge/.style={-{Stealth[length=2mm]}, semithick},
  lab/.style={font=\scriptsize, fill=white, inner sep=1.5pt}]
  \node[term] (draft) at (0.8,0) {\st{draft}};
  \node[state] (author) at (3.6,0) {\st{author}};
  \node[state] (review) at (7.8,0) {\st{review}};
  \node[term] (acc) at (12.6,0) {\st{accepted}};
  \node[pause] (poa) at (7.8,-2.4) {\st{pause-on-amend}};
  \draw[edge] (draft) -- node[lab, above] {paper stage} (author);
  \draw[edge] (author) -- node[lab, above] {built and checked} (review);
  \draw[edge] (review) -- node[lab, above] {\st{accept}, minors allowed} (acc);
  \draw[edge] (review) to[out=140, in=40, looseness=0.9] node[lab, above] {\st{amend}, rounds left} (author);
  \draw[edge] (review.south west) -- node[lab, left] {\st{amend}, rounds out} (poa.north west);
  \draw[edge] (poa.north east) -- node[lab, right] {owner edits by hand, \file{--review}} (review.south east);
  \draw[edge] (poa.east) -- ++(1.6,0) node[lab, right, text width=36mm, align=left] {or the owner raises \file{paper\_rounds} and reruns \file{paper}};
\end{tikzpicture}

\medskip

\begin{tikzpicture}[x=1cm, y=1cm,
  every node/.style={font=\footnotesize},
  state/.style={draw, rounded corners=2pt, minimum height=6.5mm, inner xsep=6pt, fill=gray!8},
  term/.style={state, fill=green!12},
  blocked/.style={state, fill=red!10},
  edge/.style={-{Stealth[length=2mm]}, semithick},
  lab/.style={font=\scriptsize, fill=white, inner sep=1.5pt}]
  \node[state, text width=30mm, align=center] (any) at (1.6,0) {any terminal word of the thread};
  \node[state] (edit) at (6.4,0) {\st{edit}};
  \node[term] (done) at (10.4,0) {\st{done}};
  \node[blocked] (blk) at (10.4,-1.8) {\st{blocked}};
  \draw[edge] (any) -- node[lab, above] {editor stage} (edit);
  \draw[edge] (edit) -- node[lab, above] {built, references clean} (done);
  \draw[edge] (edit.south east) -- node[lab, below, sloped] {two attempts failed} (blk.west);
\end{tikzpicture}
\end{center}

\noindent Terminal words of the research thread: \st{draft}, \st{pause}, \st{pause-on-iterate},
\st{pause-on-revise}. Terminal words of the paper: \st{accepted}, \st{pause-on-amend}. Endings
of the editor: \st{done}, \st{blocked}. Every \st{pause-on-x} means ``the loop named X ran out of
its budget with the judge still asking''; the owner can raise the budget and resume from the
recorded stage, or, for the paper, edit it by hand and ask for one more review. The editor runs
once for every terminal thread, whatever its ending, so that even a paused pair leaves a readable
account.

\file{stopped} and \file{BLOCKED} are not endings. \file{stopped} is a drain checkpoint (a stop
marker, a transport failure, a kill) and resumes at the recorded stage on the next start.
\file{BLOCKED} is an unreadable decision or a missing output: the reply is saved beside the
position (for the verifier, \file{verify-unreadable.txt}), the runner never re-admits the pair,
and \file{reconcile} names the one safe action.

\begin{center}
\small
\begin{tabular}{@{}lll@{}}
\toprule
stage & decisions of its judge & endings \\
\midrule
research thread & \st{draft}, \st{revise}, \st{iterate}, \st{pause} & \st{draft}, \st{pause}, \st{pause-on-iterate}, \st{pause-on-revise} \\
paper & \st{accept}, \st{amend} & \st{accepted}, \st{pause-on-amend}, \file{blocked} \\
editor & none & \st{done}, \file{blocked} \\
\bottomrule
\end{tabular}
\end{center}

\section{Transitions}

Each transition is one or more model calls, or a mechanical step by the pipeline or the owner.
``Sees'' is the part of the global state the agent can read; ``writes'' is what it may change.
Nothing else is visible or writable, which is what makes the record auditable.

\subsection*{Research thread}
\begin{footnotesize}
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{24mm} >{\raggedright\arraybackslash}p{26mm} X X >{\raggedright\arraybackslash}p{32mm}@{}}
\toprule
from $\to$ to & agent, prompt, tools & sees & writes & budget \\
\midrule
corpus $\to$ \st{scanned} & judge, \file{scan.md}, no tools & Q and P titles and abstracts, or full text & one row of \file{scan.jsonl} & one call, retried once if unparseable \\
\st{scanned} $\to$ \st{new} & none (\file{select}) & all scan rows & \file{shortlist.json}: the top cut, or every pair at or above a threshold; started pairs are kept & none \\
\st{new} $\to$ \st{peers} & none (runner) & shortlist, statuses, locks, receipts, markers & lock, \file{status.json} & seats; guard: spend plus in-flight estimate under \file{budget\_usd}, else the stop marker \\
\st{peers} $\to$ \st{peers} & every peer concurrently, \file{peer.md}, tools and web search & inputs, ledger, own and partners' directories, the scan's connexion and scores, this call's allowance & ledger entries through the helper, own directory & \file{peer\_calls} per peer and \file{peer\_seconds} shared, per round; a peer whose ready stands waits instead of calling \\
\st{peers} $\to$ \st{consolidate} & none & ledger readiness & \file{status.json} & when every peer is ready or the allowance is out; an empty ledger goes to \st{pause} \\
\st{consolidate} $\to$ \st{verify} & first peer, \file{consolidate.md}, tools & ledger, all directories, the prior note; on a repair also the verifier's corrections & \file{<pair>.tex}; after an \st{iterate} it appends to the prior note, on a repair it rewrites & \file{consolidate\_seconds}; rerun once unless the note exists \\
\st{verify} $\to$ next & verifier, \file{verify.md}, no tools & inputs, ledger, note, all inline & verdict file with the note's digest, a review entry in the ledger, \file{status.json} & \file{verify\_seconds}; rerun once on an empty reply; an unreadable verdict is saved and the pair is \file{BLOCKED} \\
\st{verify} $\to$ \st{peers} & verdict \st{iterate} & & \file{round} $+1$ & \file{rounds} (4); at the cap \st{pause-on-iterate} \\
\st{verify} $\to$ \st{consolidate} & verdict \st{revise} & the consolidator also sees the corrections & \file{repairs} $+1$ & \file{repairs} (1); at the cap \st{pause-on-revise} \\
\bottomrule
\end{tabularx}
\end{footnotesize}

\subsection*{Paper, owner and editor}
\begin{footnotesize}
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{24mm} >{\raggedright\arraybackslash}p{26mm} X X >{\raggedright\arraybackslash}p{32mm}@{}}
\toprule
from $\to$ to & agent, prompt, tools & sees & writes & budget \\
\midrule
\st{draft} $\to$ \st{review} & author, \file{author.md}, tools and web search & everything in the thread, the web, the previous review's findings if any & \file{paper/}: \file{paper.tex}, \file{references.bib}, \file{search.md} & \file{paper\_seconds} per call \\
\st{review} $\to$ next & reviewer, \file{review.md}, no tools & paper, bib, the pipeline's build and reference checks, the search record, note, ledger, inputs, all inline & \file{paper/review.json} with the paper's digest, \file{paper.json} & \file{review\_seconds}; \file{paper\_rounds} (3); at the cap \st{pause-on-amend}. \st{accept} may carry minor findings; \st{amend} only for a finding that changes a claim, its scope, attribution or correctness \\
\st{pause-on-amend} $\to$ \st{review} & the owner edits the paper by hand, then \file{paper PAIR --review} & the paper as it stands & one more review, counted as the next round & none: an owner round is outside the budget \\
\st{pause-on-x} $\to$ its loop & the owner raises the budget and reruns \file{research} or \file{paper} & the position & the loop continues from the recorded stage & the raised cap \\
terminal $\to$ \st{edit} & editor, \file{editor.md}, tools, no search & inputs and metadata, note, verdicts, ledger, all directories; on retry, the build log and the reference findings & \file{edited/} & \file{edit\_seconds}; two attempts, then \file{blocked} \\
\bottomrule
\end{tabularx}
\end{footnotesize}

\medskip\noindent Between transitions the pipeline itself, with no model, does the checks that
gate the next state: it builds the \LaTeX, verifies that every citation exists and is used and
that arXiv identifiers carry their titles, refuses an edited note whose references do not check,
records digests, and writes the position. The verifier's and reviewer's judgements are the only
decisions; every other arrow is mechanical or the owner's.

\section{The campaign}

\begin{center}
\begin{tikzpicture}[x=1cm, y=1cm,
  every node/.style={font=\footnotesize},
  state/.style={draw, rounded corners=2pt, minimum height=6.5mm, inner xsep=6pt, fill=gray!8},
  edge/.style={-{Stealth[length=2mm]}, semithick},
  lab/.style={font=\scriptsize, fill=white, inner sep=1.5pt}]
  \node[state] (fetch) at (0,0) {\file{fetch}};
  \node[state] (sources) at (2.8,0) {\file{sources}};
  \node[state] (scan) at (5.6,0) {\file{scan}};
  \node[state] (select) at (8.4,0) {\file{select}};
  \node[state] (research) at (11.4,0) {\file{research}};
  \node[state, text width=22mm, align=center] (after) at (14.8,0) {\file{paper}, \file{edit}, \file{export}};
  \draw[edge] (fetch) -- (sources);
  \draw[edge] (sources) -- (scan);
  \draw[edge] (scan) -- (select);
  \draw[edge] (select) -- (research);
  \draw[edge] (research) -- (after);
  \draw[edge] (select.south) -- ++(0,-1.2) -| node[lab, pos=0.25, below] {open-ended: \file{fetch --more}, page older papers, scan the new pairs, \file{select --min-score}} (fetch.south);
\end{tikzpicture}
\end{center}

\noindent \file{scan.jsonl} is append-only, so a longer corpus only adds rows; a threshold cut
re-selects over all rows and keeps every pair that has a thread. \file{explore} runs the loop
\file{fetch --more}, \file{scan}, \file{select --min-score} for a number of passes. The runner
then admits whatever is \st{new} on the shortlist, so research can run while the corpus grows.

\section{Budgets, and what exhausting them means}

\begin{center}
\begin{tabularx}{\textwidth}{@{}llX@{}}
\toprule
budget & scope & when exhausted \\
\midrule
\file{peer\_calls}, \file{peer\_seconds} & one peer round & consolidate on the ledger as it stands \\
\file{rounds} & the \st{iterate} loop & \st{pause-on-iterate} \\
\file{repairs} & the \st{revise} loop & \st{pause-on-revise} \\
\file{paper\_rounds} & the \st{amend} loop & \st{pause-on-amend} \\
editor attempts & the build and reference checks & \file{blocked} \\
\file{budget\_usd} & the campaign, at admission & stop marker; threads in flight finish; \st{new} pairs wait \\
\bottomrule
\end{tabularx}
\end{center}

\noindent Every model call appends a receipt, so the campaign budget is checked against the sum
of receipts, never against an estimate alone. A call that times out with no usage is charged at
\file{call\_estimate\_usd}. Owner rounds are not budgeted: the owner is the budget.

\section{Interrupts and hazards}

\begin{itemize}\setlength{\itemsep}{1pt}
  \item A stop marker (operator, guard, or first Ctrl-C) stops admission; calls in flight land
    and write their checkpoints; the runner exits; the next start resumes each pair at its
    recorded stage.
  \item No session within the grace time is a transport failure: no receipt, the pair is
    \file{stopped}, two in a row set the health marker and pause admission until a probe
    succeeds. The \file{paper} and \file{edit} commands skip the failed pair and continue.
  \item \file{reconcile} reads the position of one pair and names the one safe action: start,
    resume peers, run consolidate, run verify, or nothing; \file{--apply} performs it.
  \item The lock protects a thread from two runners. It does not protect the paper or editor
    stage from two concurrent commands on the same pair; that is an operator error the code
    does not yet catch.
\end{itemize}

\appendix
\section{Agent resources against human resources, per pair}
\label{app:resources}

\paragraph{The campaign.} The reference campaign ran on September 11-13 2026.
The two corpora were the ten most recent arXiv papers for the queries
``mechanism design'' ($Q$, dated 28 August to 4 September) and ``agentic
cooperation'' ($P$, 16 July to 3 September). A Claude Sonnet judge scanned all
100 pairs from titles and abstracts, giving each a feasibility and a gain on a
scale of 0 to 100 and a one-paragraph connexion that serves as a seed; the score
is their product, and the 14 pairs at or above 1500 were selected. Each thread
ran two Opus 5 researcher agents with web search. The caps were \file{rounds} 4,
\file{repairs} 1 and \file{paper\_rounds} 3, under a campaign budget of 200~USD.
Outcomes: 5 threads ended \textsc{draft}, 8 \textsc{pause} and 1
\textsc{pause-on-iterate}; the 5 drafts became papers, 4 accepted by the
reviewer within the run and 1 after the owner's edits; all 14 threads got a
readable note. Total: 330 model calls and 187~USD at API prices.

Agent figures are taken from the receipts of that campaign, as medians
over threads. Means are dragged up by three threads that hit time-outs and
by one paper whose seven review rounds were spread over two days. Human
figures are estimates for a postdoc-level researcher at a fully loaded cost
of about 60~USD per hour, stated so that they can be rescaled.

\begin{center}
\small
\begin{tabularx}{\textwidth}{@{}lrrrX@{}}
\toprule
agent, per pair & calls & USD & wall time & notes \\
\midrule
scan & 1 & 0.03 & 11 s & 100 pairs for 3.34 USD \\
research (peers, consolidate, verify) & 12 & 6.8 & 8 min & mean 9.8 USD and 51 min; worst thread 30 USD and 6 h \\
paper (author, review) & 4 to 11 & 3.5 & 5 min per round & 5 pairs; the worst took 14.7 USD over 7 rounds \\
readable note (editor) & 1 & 1.0 & 2 min & \\
\midrule
pair to reviewed paper & about 20 & about 12 & about 20 min & about 8 USD and 10 min for a pair that pauses \\
\bottomrule
\end{tabularx}
\end{center}

A second run of eight pairs with three peers on the Codex model gave a
research stage of 7.8 USD and 7 minutes per pair: the model choice moves
the cost little, the peer count moves the time.

\begin{center}
\small
\begin{tabularx}{\textwidth}{@{}Xrrl@{}}
\toprule
human, per pair, same deliverables & hours & USD & calendar \\
\midrule
scan two abstracts and decide & 0.25 & 15 & minutes \\
read both papers, derive, write the note & 25 to 40 & 1,500 to 2,400 & 3 to 5 days \\
a colleague verifies the note & 2 to 4 & 120 to 240 & when free \\
paper with literature search & 60 to 80 & 3,600 to 4,800 & 2 weeks \\
a referee's review & 4 to 8 & 240 to 480 & weeks of latency \\
readable write-up for outsiders & 4 & 240 & half a day \\
\midrule
pair to reviewed paper & about 100 & about 6,000 & 3 to 4 weeks \\
\bottomrule
\end{tabularx}
\end{center}

Per pair the agent pipeline is therefore roughly 500 times cheaper and 100
times faster on the calendar. At the scale of a campaign the gap is what
makes the method exist at all: scanning 100 pairs costs 3~USD against 25
human hours, and the 14 threads and 5 papers of the reference campaign cost
175~USD, about three loaded postdoc days, against something like a working
year.

Three caveats keep the comparison fair.
\begin{itemize}\setlength{\itemsep}{1pt}
  \item \emph{Not the same quality.} The accepted papers are correct
    technical remarks, the kind a good student writes in a week, not the
    pair a senior researcher would choose nor the paper they would write.
    A human would also never look at 100 pairs; taste replaces breadth.
  \item \emph{Human time does not go to zero.} The owner still reads the
    readable notes, clears the decision queue and settles the paused
    pairs: on this campaign of the order of 10 to 20 minutes per pair, plus
    an hour of attention and about 12~USD for the one paper that was
    rescued by hand. The pipeline moves human effort from producing to
    choosing and reading.
  \item \emph{Wall time is set by the slowest call, not by the sum.} Peers
    run concurrently, so a thread's clock is its longest call and its
    time-outs; the 8-minute median hides a 6-hour worst case.
\end{itemize}

\end{document}
