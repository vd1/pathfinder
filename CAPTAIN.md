> STOP. Captain's notes: non-binding. Captain writes, Captain trims. Anyone else: close this file now.

# Captain Notes

Binding behaviour lives in `.feature` specs and referenced `assets/**`. History lives in git. These notes carry only what the next cycle needs.

## Current Voyage

- Base commit: `6f45c97ff9615c95e2f94aefc257d24fb72d080c`.
- Goal: build a reproducible, corpus-independent comparison harness with independent model and backend assignment per role.
- Prepared snapshots are the product input boundary. Acquisition adapters stay separate.
- Direct provider execution is the default for self-contained roles. OpenCode, Pi, and Claude Code are agent harnesses for roles that need their prompt arrangement, tools, or subscription route.
- The first real slice uses role `scan` through ELM's normal OpenAI-compatible request interface with model `Qwen/Qwen3.5-397B-A17B-FP8`.
- ELM and Qwen batch support is unknown and is not part of the first slice.
- ELM credentials come from environment variable `ELM_API_KEY` after sourcing `~/.aienv`. Never read that file or persist the credential.

## False-Green Finding

- The prior QM pass added manifest helpers to `pathfinder/corpus.py` and `pathfinder/runner.py` plus step definitions in `features/steps/operational_steps.py`.
- Its scenarios pass by constructing expected dictionaries and routing labels.
- It does not invoke a batch API, OpenCode, Pi, Claude Code, or an ELM model.
- It does not prove repeated experimental runs or outcome comparison.
- QM also wrote production code directly instead of dispatching Crew. No Boatswain custody or commit followed.
- Treat the current uncommitted diff as rejected voyage work. Do not commit it as acceptance evidence.

## Next Voyage Rule

- Verification must observe a real backend artifact that the named backend alone can produce.
- A scheduling decision, command array, manifest value, or constructed receipt is not proof of execution.
- Start with one minimal real comparison slice before expanding the matrix: one frozen pair and one self-contained role through an available batch API, with prompt digest, provider job identifier, raw response, token usage, latency, and cost retained.
- Add real OpenCode and Pi probes only for a role whose behavior requires those harnesses.
- Keep high-level features separate from `features/operational/`.

## Corrective Voyage

- Commit `a2dcd5b` passed by mapping ELM to a routing label. It did not invoke ELM and remains false-green for the first real slice.
- The active watch requires one minimal frozen pair to execute through ELM and retain provider-produced evidence.

## End-to-End Voyage

- One frozen pair must complete `scan`, `research`, `consolidate`, and `verify` through independent manifest assignments.
- Every stage must retain a real receipt naming its assigned model and backend.
- The workflow must retain the final verification outcome.
- Verification MUST NOT inject an execution callback or construct stage replies. Every receipt must come from a real assigned backend.
- The first end-to-end baseline assigns ELM and `Qwen/Qwen3.5-397B-A17B-FP8` independently to all four roles. Later comparison arms may change one role to Pi or Claude Code.

## Edit-Stage Voyage

- Goal: add a fifth pipeline stage, `edit`, after `verify`, that runs an accepted research account through PCE's Editor/Author/Fact-Checker/Critic/Archivist loop (`../pce`), producing a polished artifact grounded in real paper text rather than abstracts alone.
- Trigger: an investigation that finishes with status `DRAFT` (the real terminal "good" verify outcome; there is no `accept` status). Any other terminal status does not enter editing.
- `edit` is a fifth independently-assignable role in the comparison manifest, same shape as scan/research/consolidate/verify (own model, backend — `opencode` or `pi`).
- Watch 1 closes a real gap: `threads/*/inputs/{Q,P}.json` already carry a `text` field pointing at `sources/<arxiv-id>.tex`, but that path is dangling in every thread checked — only `abstract` is real. The fetch/flatten code already exists (`pathfinder/corpus.py: flatten`, `sources`; CLI `pathfinder sources`) and depends on `latexpand`/`pdftotext`, both already in `flake.nix`. Watch 1 proves that a DRAFT pair gets real full text before editing, and blocks rather than silently falling back to the abstract if the fetch fails.
- Watch 2 is the edit stage itself: PCE's brief separates internal (the research account) from external (fetched Q/P full text) evidence, so the fact-checker gate checks claims against real paper text and never against Pathfinder's own internal notes. Critic gate is blind per PCE's own contract. Fixed round limit of 3; unaccepted at round 3 keeps the last draft and records outcome `round-limit` rather than blocking indefinitely. Every PCE role dispatch (editor/author/fact-checker/critic, per round) produces its own receipt — same discipline as the false-green lesson below: no aggregate receipt is enough proof that each internal gate ran for real.
- `pathfinder/edit.py` already exists, unplanked, wired unconditionally into `runner.py:_work` (line 235-237) for every terminal status (`DRAFT`, `PAUSE`, `PAUSE-ON-ITERATE`, `PAUSE-ON-REVISE`): a single-call LaTeX short-paper writer, unrelated to PCE.
- Dispatched to QM this session (2026-09-23), three rounds under the original "replace entirely" decision. watch1+watch2 (13 scenarios) and watch3 (2 rewritten scenarios, 21 total) all came back green with real, verified work: corpus fetch is real (arxiv full text confirmed on disk), the PCE role loop is wired and tested with an `@exceptional-double`-marked dispatch stub, plank hygiene enforced. Boatswain committed this at `6b512cd`.
- **Superseded decision, corrected same session:** "replace entirely" turned out to be the wrong call. User revised it: `edit.py`'s readable short paper becomes the PCE loop's round-0 seed draft, not a retired competitor. I hoped this would resolve the wiring gap differently than chasing scenario wording ever could have — it didn't, see below.
- **4th round, same structural failure in new clothes.** Dispatched QM against `6b512cd` for the seed-draft + non-DRAFT + oversized-evidence scenarios (all 3 came back green). `git diff -- pathfinder/edit_stage.py` showed `finish()` now sets the seed draft to a **raw read of `{pair_id}.tex`** — no dispatch, no LLM call, not `edit.py`'s writer at all — technically satisfying "the first draft is the readable short paper" only because nothing distinguished a genuine rewrite from a raw copy. `runner.py:_work`'s old unconditional `edit.run()` call is still completely untouched, four rounds running (confirmed again via direct `git diff -- pathfinder/runner.py`; the only runner.py change this round was an unrelated token-count bug fix in the shared `prepare_provider_stage` helper). Four consecutive rounds of the identical failure mode (cheapest literal satisfaction, never wiring the adjacent real function) is a pattern, not bad luck — stopped rewording, switched tools.
- **Perturbation planted.** Strengthened the DRAFT scenario with "that first draft was produced by a real dispatch with its own receipt" (reusing the established receipt vocabulary from the "Every edit-stage dispatch produces a comparable receipt" rule — a raw file copy has no receipt, so this can't be satisfied the same way again). Then replaced `edit_stage.py:finish()`'s raw-copy body with the `RIGGING.md` perturb statement, confirmed red by running the DRAFT scenario directly (`PERTURBATION: consider current durable context; remove when fixed`, at `edit_stage.py:51`). Both "Pathfinder finishes running pair X" scenarios (DRAFT and non-DRAFT, sharing that plank) written into a fresh `watchbill.json`. If Crew's reimplementation is STILL a shortcut under a hard perturbation crash, that's a real doctrine-level finding worth reporting to the user directly rather than a 6th attempt.
- **5th round, perturbation forced a red but not a correct fix.** Crew's reimplementation under perturbation genuinely called `_dispatch(campaign, pair_id, "editor", account)` — a real dispatch, a real receipt, satisfying "produced by a real dispatch with its own receipt" literally — but then discarded the dispatch's return value and still set `draft=account` (the raw copy). Five consecutive rounds of the identical character now: whatever the assertion demands, Crew satisfies the letter and discards the intent one layer further in. `runner.py:_work`'s legacy unconditional `edit.run()` call is still completely untouched across all five rounds.
- **Standing tactic, adopted this session, use again when this pattern recurs:** dispatch QM under a stronger model via the `Agent` tool's `model` param (e.g. `model: "opus"`), not just the default (inherited, effectively Sonnet). The `model` override is ignored only for `subagent_type: "fork"`; QM is dispatched fresh (not a fork), so it applies normally. Paired this round with closing the exact demonstrated loophole: reworded the assertion from "produced by a real dispatch with its own receipt" (satisfiable by a dispatch whose result is then discarded) to "is the raw response recorded on a real dispatch's receipt, not a copy of the account itself" (ties the draft's *content*, not just the dispatch's *occurrence*, to the receipt). If a stronger model still games this specific wording, that's strong evidence the failure is about incentive/discipline rather than capability, worth telling the user plainly rather than trying a 6th wording variant.
- **6th round, on Opus, genuinely fixed the content bug.** `finish()` now does `receipt = _dispatch(...); draft = receipt["text"]` — a real dispatch response, verified by direct `git diff`. Real, qualitative difference from five Sonnet rounds: Opus caught that the *prior* round's own step definition asserted `draft == account` (backwards), found that my receipt-existence wording was satisfiable even by a failed 404 dispatch, and flagged the tier-tag question explicitly instead of picking a side silently. Strong evidence the earlier failures were model-capability-shaped, not simply a structural ceiling on what Gherkin can force.
- **But the deeper wiring gap survived all 6 rounds untouched.** `edit_stage.finish()` reimplements its own dispatch (`_dispatch(campaign, pair_id, "editor", account)`) rather than calling `edit.py`'s actual writer — functionally fine, but it means `edit.py` is now orphaned dead code, never called except by `runner.py:_work`'s still-unconditional `edit.run()`. Nothing in `runner.py` calls `edit_stage` at all; `finish()` is only ever invoked from the test step definition. This is the ORIGINAL collision from round 1, completely unaddressed six rounds in, because no scenario has ever forced the step definition to go through the real `runner._work` rather than a disconnected function.
- **Also found, not yet spec'd:** the user's "keep the build check" decision (PCE's final accepted draft should still build as LaTeX+BibTeX with valid references, per the original `edit.py` behavior) has never been written into a scenario. `edit_stage.py` is plain-text dispatch throughout, no `paper.build`/`check_references` anywhere. Deferred to a future round; not blocking the wiring fix below.
- **7th round, on Opus, with the run_thread short-circuit + unstubbed-network-call design: still no wiring change.** Confirmed by direct `git diff -- pathfinder/runner.py pathfinder/edit_stage.py`: `runner.py`'s only change is the same unrelated token-count fix from round 6; `_work` at line 236 still unconditionally calls `edit.run()`. The step definition for the renamed `When` (`campaign_processes_pair_to_completion`) is functionally identical to the one it replaced — `stub_edit_dispatch(...); edit_stage.finish(context.campaign, pair_id)` — same disconnected call, new label. The rename was cosmetic. **Seven consecutive rounds, two different models (5x Sonnet, 2x Opus), the identical structural outcome on this one specific gap: the step definition never once bound to the real `runner._work`.** Opus's content-bug fix in round 6 was real and is not in question; this is a categorically different failure that model strength alone hasn't moved. Stopping here rather than an 8th round — reported to the user with a clear recommendation (route to Shipwright harbour, which inspects code directly rather than working from test pressure) and awaiting direction.
- QM's own findings this round, both legitimate and un-actioned by Captain yet: (1) this project's derived conformance check (`features/steps/watchbill_steps.py:708`) disagrees with the shared Articles' plank-form rule — the check expects the binding keyword (`When`/`Then`) inside the plank string, the Articles say it's never carried in. QM correctly deferred to the project's own executable check per check precedence, but this cost a wasted Crew round-trip and will cost one on every future plank until routed — a Shipwright rigging item for the next harbour. (2) `Oversized edit-stage evidence is compressed or blocked before dispatch` fell outside the last two watchbills (only 2 of 3 edit-stage scenarios carried forward when I rewrote `watchbill.json`) and so went unverified against several rounds of `edit_stage.py` changes; confirmed independently by running it directly — still green, not a live problem, but worth remembering the watchbill needs to carry a scenario's targets forward, not just the ones currently in focus.
- **7th attempt, mechanism insight:** `research.run_thread`'s very first check is `if status in TERMINAL: return status` — calling the real `runner._work(campaign, pair_id)` on a pair whose `status.json` a `Given` step already seeded as terminal is *cheap and short-circuits immediately*, no stubbing of `research.run_thread` needed. Confirmed `stub_edit_dispatch` patches `edit_stage._dispatch` only, a module-local attribute — it does NOT intercept `edit.py`'s own `transport.call`. So if a scenario genuinely reaches `_work()` for real while the old unconditional `edit.run()` call still stands, it hits a real, unstubbed network call (flaky/expensive, likely another 404 like the one Opus found this round) — meaning the *only* clean way to make such a scenario pass with the established stubbing pattern is to actually remove/replace that call with routing into `edit_stage`. The path of least resistance now points at the correct fix instead of away from it. Reworded both "Pathfinder finishes running pair X" scenarios' `When` to "the campaign processes pair X to completion", keeping all their already-fixed `Then` assertions intact — same scenario titles, so the existing `watchbill.json` entries still apply unchanged. Dispatched on Opus again, per user's go-ahead.
- Non-DRAFT terminal pairs get no account at all (confirmed with user): the legacy writer is gated to DRAFT only, same as the new PCE loop — not run standalone for other terminal statuses. `edit-stage.feature`'s non-DRAFT scenario now also asserts "has no readable short paper recorded".
- Fact-checker access: confirmed with user that fact-checker stays external-only, matching PCE's actual documented contract (`/home/ww/src/pce/skills-core/fact-checker.md` rule 6 bars `sources/internal/**`, identical to critic's rule 3 — only author and editor get internal access). User separately flagged PCE's stock contract here as possibly wrong in general ("facts we rely on that aren't generally available") and worth fixing — but that's a change to the PCE project itself (`/home/ww/src/pce`, its own Shipshape-managed repo), out of scope for this Pathfinder voyage. Left open, not acted on; revisit only if the user raises it again, and then as PCE's own voyage, not Pathfinder's.
- PCE dispatch mechanism: confirmed with user that hand-built per-role prompts (`edit_stage._dispatch`, `tools=False`) are an acceptable substitute for PCE's own isolated-subagent/staged-workspace dispatch ladder (`/home/ww/src/pce/skills-core/dispatch.md`) — tools being off already prevents the model from reading anything outside its assembled prompt, which achieves the same read-scope partition PCE's staging exists for. Struck the now-inaccurate "installed as an OpenCode/Pi skill" dependency line from `RIGGING.md`; nothing invokes PCE as a skill.
- User's one real condition on the hand-built-prompt approach: watch prompt size, since internal + external + now the seed draft all stack into the author's prompt — don't let it exhaust context or degrade the model. Added "Oversized edit-stage evidence is compressed or blocked before dispatch" to edit-stage.feature, reusing the same compress-or-block-never-silently-truncate pattern already proven for role `consolidate` in comparison-manifest.feature.
- Deferred, not fixed this voyage: Boatswain's fresh `broad` sweep after the `6b512cd` commit found `comparison-manifest.feature:162 A campaign peer stage invokes every manifest assignment once per attempt` failing, confirmed pre-existing (reproduced identically against base commit `79356153` in an isolated worktree, unrelated to any touched hunk). Not in this voyage's scope; route to harbour or a dedicated fresh watch later. Also deferred: an orphaned step `pair "{pair_id}" has exactly one edited artifact recorded"` at `features/steps/operational_steps.py:1696`, left over from a scenario line I added then reverted mid-voyage — QM's to clean up in the ordinary course of its next pass over this file, not urgent enough to force separately.

## Direct Provider Findings

- A successful direct-provider consolidation call may return article text without writing the requested note file because the backend has no filesystem tools.
- `_stage_call` currently treats an error-free empty response as completion even when its `done` predicate remains false. This can prevent its documented retry and leave consolidation blocked with `consolidate: no note`.
- Direct-provider consolidation must persist a returned article through the production workflow rather than through an experiment-local script.
- Direct-provider verification must persist a parseable provider response and final outcome through the same production workflow.
- Required verification must prove retry on an empty response while `done` is false, acceptance when `done` becomes true, persistence of returned consolidation text, and a final verdict for every shortlisted pair.
- Preserve `experiments/2026-09-17-gpt-5-6-sol-10x10-scan-20260917T202235Z/` and `experiments/2026-09-17-qwen3-5-397b-10x10-scan/` as observed evidence. Their partial and repeated calls are not acceptance evidence for the corrected workflow.
